import Foundation

struct CharacterModelView {
    let id: Int
    let name: String
    let description: String
    let imageURL: URL?
    let comics: Int
    let stories: Int
    let events: Int
    let series: Int
}
