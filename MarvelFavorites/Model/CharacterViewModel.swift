import Foundation

struct CharacterViewModel {
    let name: String
    let description: String
    let imageURL: URL?
    let comics: Int
    let stories: Int
    let events: Int
    let series: Int
}
